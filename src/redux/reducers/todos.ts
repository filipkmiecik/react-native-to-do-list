import { ADD_TODO, REMOVE_TODO } from "../actions";

const todos = (state = [], action: any) => {
  switch (action.type) {
    case ADD_TODO: {
      return;
    }
    case REMOVE_TODO: {
      return;
    }
  }
};

export default todos;
